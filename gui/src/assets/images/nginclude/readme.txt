All svgs in here will be put into the Angular template cache. The /nginclude/ subdirectory is excluded during
compilation, so do not include it when referencing the images (use /images/<filename.svg>).
